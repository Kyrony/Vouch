# Menu fonts

Used by the Godot `VouchMenu` port (F5 / Lobby home).

| File | Role | License |
| --- | --- | --- |
| `Cinzel-Regular.ttf` / `Cinzel-Bold.ttf` | UI / gold **V** (`--font-vouch-ui`) | SIL OFL 1.1 — `OFL-Cinzel.txt` |
| `SpecialElite-Regular.ttf` | Stone **OUCH** (`--font-vouch-stone`) | Apache 2.0 — `LICENSE-SpecialElite.txt` |

Closest DynamicFonts if these fail to load: `ThemeDB.fallback_font`.
