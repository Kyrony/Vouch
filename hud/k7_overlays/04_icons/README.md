# K7 neon item icons

Follow-up drop. Expected stems (both `64/` and `128/`):

`key`, `firearm`, `shovel`, `crowbar`, `bandage`, `battery_pack`, `lockpick`, `evidence`, `rope`, `fuse`, `medkit`, `flashlight`

Until those PNGs land, `HudIconPack.rail_texture` uses `res://assets/horror/hud/icon_*.png`.
